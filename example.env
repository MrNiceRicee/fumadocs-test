# Environment variables for fumadocs-test

# Node environment: 'development' or 'production'
NODE_ENV=development

# --- GitHub Content Source (Required for NODE_ENV=production) ---
# The owner (username or organization) of the GitHub repository
GITHUB_OWNER=yourusername

# The name of the GitHub repository
GITHUB_REPO=your-repo-name

# The path in the repository where your docs content is located
# For example, if your docs are in /content/docs/, set this to "content/docs"
GITHUB_CONTENT_PATH=content/docs

# The branch to fetch content from
GITHUB_CONTENT_BRANCH=main

# GitHub personal access token (recommended for production)
# This increases API rate limits and allows access to private repositories
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token